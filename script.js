const model = require('./models');
const { Task, Project, User, Address } = model;


(async () => {
    // const tasks = await Task.findOne({include: [{all:true}]});
    // console.log(tasks);

    // Find address of user
    // const user = await User.findOne({include: [{all:true}]});
    // console.log(user.Address);
})();